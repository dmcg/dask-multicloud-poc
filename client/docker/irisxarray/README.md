Builds a Docker image with dask, iris, xarray, and
[dask-worker-pools](https://github.com/dmcg/dask-worker-pools).

Publish with

```bash
docker login --username=dmcg
docker push dmcg/irisxarray:latest
```